# emacs keybinds in zsh
bindkey -e

# need to explicity enable editor functionality in zsh
autoload -z edit-command-line
zle -N edit-command-line
bindkey "^g" edit-command-line

alias vim=nvim
alias ls='eza -lh --group-directories-first --icons=auto'
alias claude="/Users/yiyuanh/.claude/local/claude"

export EDITOR=nvim
export VISUAL=nvim

export FZF_DEFAULT_OPTS="$FZF_DEFAULT_OPTS \
    --highlight-line \
    --info=inline-right \
    --ansi \
    --layout=reverse \
    --border=none \
    --color=bg+:#353749,bg:#1e1e2e,spinner:#f5c2e7,hl:#94e2d5 \
    --color=fg:#cdd6f4,header:#89b4fa,info:#f9e2af,pointer:#f5c2e7 \
    --color=marker:#94e2d5,fg+:#cdd6f4,prompt:#89b4fa,hl+:#94e2d5 \
    --color=selected-bg:#353749 \
    --color=border:#313244,label:#cdd6f4"

# Update tmux environment if inside tmux session
if [ -n "$TMUX" ]; then
    tmux set-environment -g FZF_DEFAULT_OPTS "$FZF_DEFAULT_OPTS"
fi

# Set up fzf key bindings and fuzzy completion
source <(fzf --zsh)

eval "$(starship init zsh)"
eval "$(/Users/yiyuanh/.local/bin/mise activate zsh)"
eval "$(zoxide init zsh)"

# emacs keybinds in zsh
bindkey -e

# Keep 1000 lines of history within the shell and save it to ~/.zsh_history:
export HISTSIZE=10000
export SAVEHIST=10000
export HISTFILE=~/.zsh_history

setopt INC_APPEND_HISTORY   # Write each command to the file immediately
setopt SHARE_HISTORY         # Read new commands from the file too (implies INC_APPEND_HISTORY)

# need to explicity enable editor functionality in zsh
autoload -U edit-command-line
zle -N edit-command-line
bindkey '^g' edit-command-line

alias vim=nvim
alias ls='eza -lh --group-directories-first'

export EDITOR=nvim
export VISUAL=nvim

export FZF_DEFAULT_OPTS="$FZF_DEFAULT_OPTS \
                         --highlight-line \
                         --info=inline-right \
                         --ansi \
                         --layout=reverse \
                         --border=none \
    --color=bg+:#ddf4ff,bg:#ffffff,spinner:#8250df,hl:#cf222e \
    --color=fg:#1f2328,header:#0969da,info:#0969da,pointer:#cf222e \
    --color=marker:#1b7c83,fg+:#1f2328,prompt:#cf222e,hl+:#cf222e \
    --color=selected-bg:#ddf4ff \
    --color=border:#d0d7de,label:#1f2328"

# Set up fzf key bindings and fuzzy completion
source <(fzf --zsh)
eval "$(starship init zsh)"
eval "$(mise activate zsh)"
eval "$(zoxide init zsh)"
